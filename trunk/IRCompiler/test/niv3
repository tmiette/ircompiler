import imports.Complexe;
import java.util.ArrayList;

function ArrayList liste()
  ArrayList list = ArrayList();
  list.add(Complexe(8,8));
  list.add(Complexe(10,10));
  return list;
end

string s = "Affichage de ";

foreach Complexe y in liste() do
  print s + y + ".";
done
